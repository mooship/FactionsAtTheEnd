# Set the default behavior to automatically normalize line endings
* text=auto

# Force all files to use LF on check-in, but CRLF on checkout for Windows
* text eol=lf

# These files should always use LF
*.sh text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.md text eol=lf

# These files should always use CRLF
*.bat text eol=crlf
*.ps1 text eol=crlf
